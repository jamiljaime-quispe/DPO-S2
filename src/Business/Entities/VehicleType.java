package Business.Entities;

/**
 * Enumerates the categories of vehicles the parking system supports. This enum keeps the allowed values in
 * one place, which makes the rest of the code easier to read and harder to misuse.
 * <p>
 * The enum keeps the allowed options in one place so the project does not rely on repeated text values.
 * </p>
 */
public enum VehicleType {
	/**
	 * Handles this part.
	 * <p>
	 * This helper keeps the step named and separate, which makes the larger operation easier to read and
	 * follow.
	 * </p>
	 */
	MOTORCYCLE,
	/**
	 * Handles this part.
	 * <p>
	 * This helper keeps the step named and separate, which makes the larger operation easier to read and
	 * follow.
	 * </p>
	 */
	CAR,
	/**
	 * Handles this part.
	 * <p>
	 * This helper keeps the step named and separate, which makes the larger operation easier to read and
	 * follow.
	 * </p>
	 */
	LARGE_VEHICLE;
}
